extends AudioStreamPlayer

#func _ready():
#	if !get_tree().current_scene.is_in_group("TitleScreen"):
#		play()

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
